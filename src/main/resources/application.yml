spring:
  datasource:
    url: jdbc:postgresql://dpg-clml8effeb2c73e8r4qg-a.singapore-postgres.render.com/post_wws2
    username: post_wws2_user
    password: e7XBsjEXI8sHdFY3TQmsfoU6xZRSAJiF
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.hibernate.SQL: debug
#    org.springframework: debug

server:
  servlet:
    session:
      cookie:
        same-site: none
        secure: true